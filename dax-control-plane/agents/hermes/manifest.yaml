name: hermes
version: 1
supported_runtimes:
  - native
  - docker
  - kvm
  - remote
default_runtime: docker
volumes:
  - hermes_data
resources:
  cpus: 2
  memory: 4G
security:
  non_root: true
  no_new_privileges: true
  cap_drop_all: true
network:
  mode: isolated
healthcheck:
  enabled: true
