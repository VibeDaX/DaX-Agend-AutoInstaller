agent: "hermes"
runtime: "docker"
services:
  - name: "hermes"
    image: "dax/hermes:baseline"
    volumes:
      - "hermes_data"
    env:
      - "OLLAMA_HOST=http://ollama:11434"
  - name: "ollama"
    image: "ollama/ollama:latest"
    volumes:
      - "ollama_data"
    gpu: "auto"
