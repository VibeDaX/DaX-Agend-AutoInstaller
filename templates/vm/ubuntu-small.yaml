name: "dax-ubuntu-small"
os: "ubuntu"
ram: 4096
cpus: 2
disk: 40
network: "default"
iso: "/isos/ubuntu-24.04.iso"
cloud_init:
  enable: true
  user: "dax"
  ssh_authorized_keys:
    - "ssh-ed25519 AAAA..."
post_install:
  - "apt update && apt install -y docker.io"
